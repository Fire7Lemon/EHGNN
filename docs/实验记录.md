# 实验记录笔记（PubMed / DBLP 等节点分类）

**分支：`reproduce-baseline`。** 本节描述的 **`main.py` 默认行为**为 RW 后 **`Counter.most_common(K)` 频次 Top-K**；**不包含** `neighbor_strategy` / hybrid / temp 等 CLI。**邻居策略相关脚本与代码均在分支 `neighbor-strategy-dev`。**  
**从 GitHub clone 后的 conda、CUDA、PyTorch/DGL 安装及 `data/`/`results/` 说明**：见 **`环境配置说明.md`**。

以下内容包含两部分口径：**(A)** 服务器 **core reproduction** 归档（**`server_results/2026-05-16_core_reproduction/`**，综述 **`核心结果总结.md`**）中的 **正式 5-seed** 数值；**(B)** 其它条目（如消融、邻居策略历史 sweep）仍为 **`Node Classification/results/`** 等本地历史快照，文中已标注来源。**不向其它数据集或论文表格做强泛化推断**。

---

## 0. 论文实验覆盖矩阵（脚本映射）

| 论文内容 | 仓库脚本 / 说明 |
|----------|----------------|
| **Table V**（NC 主结果） | `Node Classification/run_pubmed_5seeds.py`、`run_dblp_5seeds.py`、`run_yelp_5seeds.py`（默认 5 seeds） |
| **Table VI**（LP 主结果） | `Link Prediction/run_*_lp_5seeds.py` |
| **Table VII**（消融） | `Node Classification/run_nc_ablation_all_datasets.py`、`Link Prediction/run_lp_ablation_all_datasets.py` |
| **Fig.2**（K → NC） | `Node Classification/run_nc_sensitivity_k.py` |
| **Fig.3**（K → LP） | `Link Prediction/run_lp_sensitivity_k.py` |
| **Fig.4**（α → NC） | `Node Classification/run_nc_sensitivity_alpha.py` |
| **Fig.5**（α → LP） | `Link Prediction/run_lp_sensitivity_alpha.py` |
| **Table VIII**（walk_num / w） | `Node Classification/run_nc_sensitivity_walk_num.py`、`Link Prediction/run_lp_sensitivity_walk_num.py` |
| **Fig.6**（meta-path 数量） | **未实现自动化**：见 **`元路径实验待办.md`** |
| **Table IX**（meta-path 权重） | **未实现自动化**：同上 |
| **Table X**（HPPR strategy） | **未实现**：见 **`HPPR策略研究待办.md`** |
| **Table V/VI Runtime / Memory** | 训练日志：`Total training time`；内存：**无内置 profiler**，用 `scripts/run_server_resource_monitor.sh` 外挂采样 |
| **汇总索引** | `python scripts/collect_result_summaries.py` → 仓库根 `results/ALL_SUMMARIES_INDEX.md`（目录由 **`**/results/`** 规则忽略，不提交 Git） |

### 0.1 执行状态说明（与「已完成数值」区分）

下列 **core reproduction** 完成与否以服务器归档 **`server_results/2026-05-16_core_reproduction/`**（综述 **`核心结果总结.md`**）为准；**扩展实验（消融 / 敏感性）本批未运行**，脚本仍为 **Prepared**。历史本地 `results/` 若与归档不一致，**以归档 summary 为正式 core 口径**。

| 条目 | 状态 | 说明 |
|------|------|------|
| PubMed NC 5-seed | **Completed** | 归档：`Node Classification/results/pubmed_5seeds/pubmed_5seeds_summary.txt`（相对路径均相对于 **`server_results/2026-05-16_core_reproduction/`**）；详见 §0.4、§2 |
| DBLP NC 5-seed | **Completed** | 归档：`Node Classification/results/dblp_5seeds/summary.txt`；详见 §0.4、§3 |
| Yelp NC 5-seed | **Completed** | 归档：`Node Classification/results/yelp_5seeds/summary.txt`；详见 §0.4、§4 |
| PubMed LP 5-seed | **Completed** | 归档：`Link Prediction/results/pubmed_lp_5seeds/summary.txt`；详见 §0.4、§8 |
| DBLP LP 5-seed | **未完成（运行中断，需补跑）** | `logs/server_core_reproduction.log` 记载 **`[FAIL] return_code=-15`**；仅有不完整 **`Link Prediction/results/dblp_lp_5seeds/dblp_lp_seed_42.log`**，**无** `summary.txt` / `summary.csv`。按编排记录视为 **进程非正常结束 / 运行中断**，**不**将返回码解释为「方法失败」。 |
| Yelp LP 5-seed | **Missing** | 归档内 **未找到** `yelp_lp_*` 结果目录或 summary |
| MAG240M | **Missing（本批）** | 归档内 **未发现**完整训练或 checklist 所列证据文件 |
| Table VII / Fig.2–5 / Table VIII（消融与敏感性） | **本批未跑（extended）** | 脚本仍 **Prepared**；输出约定见 **`项目结构与使用说明.md`** §7 |
| Fig.6 / Table IX / Table X | **Blocked-TODO** | 仅文档规划，无本分支一键脚本 |

**口径**：**Smoke ≠ 论文主结果**；**3 seeds ≠ 5 seeds**；运行中断 / 无 summary **≠** 方法失败（除非日志另有明确归因）。

---

### 0.2 本地 smoke test（Link Prediction）

| 脚本 | 已知记录 |
|------|----------|
| `run_pubmed_lp_smoke.py` | 曾有「管线跑通」记录（极少 epoch）；**不**据此宣称 Table VI 复现完成 |
| `run_dblp_lp_smoke.py`、`run_yelp_lp_smoke.py` | **未全员验证**：笔记本可能存在 **OOM / 过长预计算**；归 **待服务器或高配验证**，不作为负面结论 |

**README LP** 未写 **`walk_num`**：LP 入口默认 **`--walk_num 100`**；敏感性脚本 **显式传 `--walk_num`**。是否与论文 Table VIII 默认叙述完全一致：**不确定**，以服务器跑出对照为准。

---

### 0.3 服务器待跑 / TODO 清单

| 优先级 | 内容 |
|--------|------|
| P0 | **补全 core**：**DBLP LP**（5 seeds，生成 summary）、**Yelp LP**（5 seeds）；**MAG240M** 按脚本链单独落地（归档 **`server_results/2026-05-16_core_reproduction`** 未见完整 MAG240M）。已有：**PubMed/DBLP/Yelp NC** 与 **PubMed LP** 正式 5-seed（见 §0.4）。 |
| P1 | `scripts/server_run_extended_experiments.sh`：Table VII / Fig.2–5 / Table VIII（**本批 core 未跑**） |
| P2 | `scripts/run_server_resource_monitor.sh`：长跑配套资源日志 |
| P3 | `collect_result_summaries.py`：跑完后生成索引 Markdown |
| 阻塞项 | Fig.6、Table IX、Table X：**`元路径实验待办.md`**、**`HPPR策略研究待办.md`**（与本文件同在 **`docs/`**），需后续最小代码补丁才可一键复现 |

**阶段说明**：本地可做 smoke；**本轮 core reproduction 正式数值**已体现在 **`server_results/2026-05-16_core_reproduction/`**（见 §0.4）。扩展实验仍默认服务器 **`server_run_extended_experiments.sh`**。详见 **`项目结构与使用说明.md`** §10。

---

### 0.4 服务器 Core Reproduction 正式结果（2026-05-16 归档）

- **归档根目录**：`server_results/2026-05-16_core_reproduction/`  
- **性质**：**core reproduction**（NC/LP 主干）；**不包含**本批内的消融或参数敏感性实验（扩展实验见 **`server_run_extended_experiments.sh`**，本归档未含其产物）。  
- **综述文件**：`server_results/2026-05-16_core_reproduction/核心结果总结.md`  
- **Seeds（均为 5）**：`42, 3407, 2026, 6666, 8888`  

下列指标命名：**Macro-F1**（Macro F1-score，宏平均 F1 值）、**Micro-F1**（Micro F1-score，微平均 F1 值）对应汇总文件中的 `best_test_macro` / `best_test_micro`（best）与 `final_test_macro` / `final_test_micro`（final）；**AUC**（Area Under the Curve，曲线下面积）、**AP**（Average Precision，平均精确率）对应 LP 汇总中的 `best_test_auc` / `best_test_ap` 与 `final_test_auc` / `final_test_ap`。

**Node Classification — 聚合（mean ± std，n=5）**

| 数据集 | Best Macro-F1 | Best Micro-F1 | Final Macro-F1 | Final Micro-F1 | 平均训练时间 (s) | 汇总来源 |
|--------|---------------|---------------|----------------|----------------|------------------|----------|
| PubMed | 0.634410 ± 0.031795 | 0.655814 ± 0.031417 | 0.599828 ± 0.044462 | 0.623256 ± 0.041601 | 3.2780 | `…/pubmed_5seeds/pubmed_5seeds_summary.txt` |
| DBLP | 0.155080 ± 0.011343 | 0.391460 ± 0.014052 | 0.156740 ± 0.022714 | 0.386320 ± 0.022936 | 5.5213 | `…/dblp_5seeds/summary.txt` |
| Yelp | 0.666300 ± 0.000000 | 0.875820 ± 0.000045 | 0.666300 ± 0.000000 | 0.875800 ± 0.000000 | 1512.8481 | `…/yelp_5seeds/summary.txt` |

（上表路径中 `…` = `server_results/2026-05-16_core_reproduction/Node Classification/results`。）

**PubMed NC**：best seed **8888**，worst **6666**（按 best Macro-F1）。**DBLP NC**：best **6666**，worst **3407**。**Yelp NC**：best/worst 汇总均为 **42**（并列）。

**Link Prediction — PubMed LP — 聚合（mean ± std，n=5）**

| Best AUC | Best AP | Final AUC | Final AP | 平均训练时间 (s) | 汇总来源 |
|----------|---------|-----------|----------|------------------|----------|
| 0.554940 ± 0.005881 | 0.528140 ± 0.006090 | 0.533960 ± 0.001596 | 0.516000 ± 0.000520 | 1833.2327 | `server_results/2026-05-16_core_reproduction/Link Prediction/results/pubmed_lp_5seeds/summary.txt` |

best seed **3407**，worst **8888**（按 best AUC）。

**本批未完成 / 缺失（勿写成已完成）**

| 项 | 说明 |
|----|------|
| DBLP LP | 仅不完整单 seed 日志；主日志 `return_code=-15`；无 LP summary |
| Yelp LP | 未找到结果目录或 summary |
| MAG240M | 未发现完整结果 |
| 消融 / 敏感性 | 本批 core 未运行 |

---

## 1. PubMed「标准」复现口径

与仓库根目录 **`../README.md`** 中 PubMed 一行一致的超参：`α=0.7, K=20, lr=1e-3, dropout=0.4, hidden=256, layers=4, batch_size=3000`。

单次运行可通过：

```bash
cd Node Classification
python main.py --dataset PubMed --seed 42
```

说明：`results/pubmed_nc_result.txt` 保存**最后一次** PubMed `main.py` 运行的快照（会被下一次 PubMed 运行覆盖）；字段含 `seed`、best/final F1、训练时间与若干固定超参行（无邻居策略开关）。

---

## 2. 五种子结果（PubMed NC，`pubmed_5seeds`）

**正式 core reproduction 口径**来源：`server_results/2026-05-16_core_reproduction/Node Classification/results/pubmed_5seeds/pubmed_5seeds_summary.txt`（聚合见 §0.4）。列名为脚本导出：`best_macro` / `best_micro` 即 **Macro-F1** / **Micro-F1**（测试集 best）；`final_*` 同理。

| seed | best_macro | best_micro | final_macro | final_micro |
|------|------------|------------|-------------|-------------|
| 42 | 0.624693 | 0.639535 | 0.599516 | 0.616279 |
| 3407 | 0.626926 | 0.651163 | 0.537022 | 0.569767 |
| 2026 | 0.626469 | 0.651163 | 0.597079 | 0.627907 |
| 6666 | 0.605063 | 0.627907 | 0.602878 | 0.616279 |
| 8888 | 0.688898 | 0.709302 | 0.662645 | 0.686047 |

聚合（n=5）：best Macro-F1 **0.634410 ± 0.031795**；best Micro-F1 **0.655814 ± 0.031417**；final Macro-F1 **0.599828 ± 0.044462**；final Micro-F1 **0.623256 ± 0.041601**。平均训练时间 **3.2780 s**（见汇总文件 `Mean training time`）。

**备注**：该批汇总中训练时长为秒级；若与预期数据规模不一致，请在写入论文材料前核对环境与数据装载路径（数值仅摘自上述归档 summary）。

---

## 3. DBLP Node Classification baseline（5 seeds，服务器 core reproduction）

**分支：`reproduce-baseline`**；**不包含** `neighbor_strategy` / hybrid / temp 等优化 CLI。

来源：`server_results/2026-05-16_core_reproduction/Node Classification/results/dblp_5seeds/summary.txt`。**seeds = [42, 3407, 2026, 6666, 8888]**（n=5）。

**工程观察**：DBLP 在 **`load_dblp` 与 meta-path RW 相似度预计算**阶段耗时相对 PubMed **明显更长**；下表训练时间仅对应 **`Total training time`** 口径的均值（`avg training time (s)`），**不包含**数据加载与 RW 预计算时间。

| seed | best_macro | best_micro | final_macro | final_micro | train_time_s |
|------|------------|------------|-------------|-------------|---------------|
| 42 | 0.148400 | 0.393200 | 0.132100 | 0.350400 | 5.2367 |
| 3407 | 0.141700 | 0.367500 | 0.140200 | 0.376100 | 5.6262 |
| 2026 | 0.157900 | 0.401700 | 0.158200 | 0.401700 | 5.3690 |
| 6666 | 0.171900 | 0.401700 | 0.190600 | 0.401700 | 5.6994 |
| 8888 | 0.155500 | 0.393200 | 0.162600 | 0.401700 | 5.6752 |

| 指标 | mean ± std |
|------|------------|
| best_test_macro | 0.155080 ± 0.011343 |
| best_test_micro | 0.391460 ± 0.014052 |
| final_test_macro | 0.156740 ± 0.022714 |
| final_test_micro | 0.386320 ± 0.022936 |

- **avg training time (s)**：**5.5213**  
- **best seed（按 best_test_macro）**：6666  
- **worst seed（按 best_test_macro）**：3407  

---

## 4. Yelp Node Classification baseline（5 seeds，服务器 core reproduction）

**分支：`reproduce-baseline`**；**不包含** `neighbor_strategy` / hybrid / temp；入口为 **`main_yelp.py`**（多标签：**sigmoid + BCELoss**）。

来源：`server_results/2026-05-16_core_reproduction/Node Classification/results/yelp_5seeds/summary.txt`。**seeds = [42, 3407, 2026, 6666, 8888]**（n=5）。

| seed | best_macro | best_micro | final_macro | final_micro | train_time_s |
|------|------------|------------|-------------|-------------|---------------|
| 42 | 0.666300 | 0.875800 | 0.666300 | 0.875800 | 1514.9360 |
| 3407 | 0.666300 | 0.875800 | 0.666300 | 0.875800 | 1510.1052 |
| 2026 | 0.666300 | 0.875800 | 0.666300 | 0.875800 | 1507.0499 |
| 6666 | 0.666300 | 0.875800 | 0.666300 | 0.875800 | 1528.9407 |
| 8888 | 0.666300 | 0.875900 | 0.666300 | 0.875800 | 1503.2089 |

| 指标 | mean ± std |
|------|------------|
| best_test_macro | 0.666300 ± 0.000000 |
| best_test_micro | 0.875820 ± 0.000045 |
| final_test_macro | 0.666300 ± 0.000000 |
| final_test_micro | 0.875800 ± 0.000000 |

- **avg training time (s)**：**1512.8481**  
- **best seed / worst seed（按 best_test_macro）**：汇总均为 **42**（并列）。

**任务与度量（事实）**：`utils.accuracy(..., dataset='Yelp')` 对 **sigmoid 输出先以 0.5 阈值二值化**，再按样本计算 sklearn F1 后平均（见 **`Node Classification/utils.py`**）。

**异常现象（记录，不夸大）**：五 seed 下 **`train_time_s` 不同**，但测试集汇总 **Macro-F1 / Micro-F1 几乎恒定**（Macro std=0，Micro std 极小）。**不应**把 **std=0** 直接解读为「Yelp 模型强稳定」或「复现极其可靠」。  

**后续可查（建议）**：检查测试集 **sigmoid 分布**、**阈值敏感性**、以及是否需要与论文对齐的 **多标签整体评估方式**（不改代码的讨论项）。

**历史 3-seed 目录**：仓库若仍存在 **`results/yelp_3seeds/`**（**`run_yelp_3seeds.py`**），仅作 **selected 3 seeds** 兼容，**不得**与上表 5-seed 正式口径混写。

---

## 5. 消融实验（3 seeds: 42, 3407, 2026）

**说明**：以下为 **历史本地** PubMed 消融汇总；**服务器 batch `server_results/2026-05-16_core_reproduction/` 未包含 Table VII 全量消融**。扩展实验待 **`server_run_extended_experiments.sh`**。

来源：`results/pubmed_ablation/summary.txt`。

| Method | Macro-F1 | Micro-F1 |
|--------|----------|----------|
| EHGNN（full） | 0.6032 ± 0.0107 | 0.6202 ± 0.0134 |
| w/o L2 | 0.5920 ± 0.0103 | 0.6085 ± 0.0067 |
| w/o MWeight | 0.5558 ± 0.0301 | 0.5775 ± 0.0242 |
| w/o TWeight | 0.5753 ± 0.0313 | 0.5969 ± 0.0178 |
| Random Neighbor | 0.6227 ± 0.0099 | 0.6395 ± 0.0116 |

相对 full，`Random Neighbor`（`--r_neighbor`）在汇总脚本定义的 Δ 意义上 **Macro/Micro 更高**（汇总文件中说明：Random 改变的是 RW 邻居抽样，不应与删权重消融混为一谈）。

---

## 6. hybrid / temp / neighbor_strategy 扫描（优化分支归档）

**不在 `reproduce-baseline`：** 下列批量脚本与 CLI 仅存在于 **`neighbor-strategy-dev`**（例如 `run_pubmed_neighbor_strategy.py`、`run_pubmed_hybrid_ratio_sweep.py`）。本分支代码已移除对应参数。

以下为曾在优化分支下生成的 **`results/pubmed_neighbor_strategy/summary.txt`** 摘录，**仅作历史对照**，不代表当前分支可复跑：

| Strategy | Macro-F1 | Micro-F1 | ΔMacro vs freq | ΔMicro vs freq |
|----------|----------|----------|----------------|----------------|
| freq | 0.6019 ± 0.0067 | 0.6202 ± 0.0067 | 0.0000 | 0.0000 |
| random | 0.6107 ± 0.0099 | 0.6279 ± 0.0116 | +0.0089 | +0.0078 |
| hybrid (0.8) | 0.5981 ± 0.0043 | 0.6202 ± 0.0067 | −0.0037 | 0.0000 |
| hybrid (0.5) | 0.6053 ± 0.0117 | 0.6202 ± 0.0134 | +0.0034 | 0.0000 |
| temp (0.5) | 0.5995 ± 0.0083 | 0.6163 ± 0.0116 | −0.0024 | −0.0039 |
| temp (1.0) | 0.6016 ± 0.0097 | 0.6163 ± 0.0116 | −0.0002 | −0.0039 |
| temp (2.0) | 0.6010 ± 0.0073 | 0.6163 ± 0.0116 | −0.0008 | −0.0039 |

---

## 7. 与 reproduce-baseline 对齐的观察

- **论文复现默认**：频次 Top-K（无双参数策略扫描）。  
- **`--r_neighbor`**：消融脚本「Random Neighbor」条目仍在 `run_pubmed_ablation.py` 中使用，含义不变。  
- 上表及其他优化分支结论 **不得** 当作本分支 `main.py` 的默认行为说明。

---

## 8. Link Prediction：本地 smoke；服务器正式 5 seeds（core 归档状态）

**口径**：本地阶段 **只做 smoke test** 或少量 selected seeds；**不把 smoke 输出当作论文完整复现指标**。正式 **5 seeds**：`[42, 3407, 2026, 6666, 8888]`。

**服务器 core reproduction（2026-05-16 归档）**：**PubMed LP** 已有完整 **`summary.txt` / `summary.csv`**（聚合见 §0.4），来源 `server_results/2026-05-16_core_reproduction/Link Prediction/results/pubmed_lp_5seeds/summary.txt`。**DBLP LP**：主日志记载 **`return_code=-15`**，仅 **`dblp_lp_seed_42.log`** 且未完成全程，**无 summary——未完成，需补跑**（不解释为方法失败）。**Yelp LP**：归档内 **未找到** 结果目录或 summary。

**日志解析**：批量脚本统一解析文末三行 **`Best Test AUC …, AP …, Epoch …`**、**`Final Epoch …`**、**`Total training time: … s`**。训练步内的 **`precision`** 与文末 **`AP`** 均指 **AP（Average Precision，平均精确率）**，不是 Accuracy。

**本地 smoke（`epochs=3`，`val_epochs=1`，`seed=42`，不追求论文指标）**，均在 **`Link Prediction/`** 下：

| 数据集 | 脚本 | 日志目录 |
|--------|------|----------|
| PubMed | `python run_pubmed_lp_smoke.py` | `results/pubmed_lp_smoke/` |
| DBLP | `python run_dblp_lp_smoke.py` | `results/dblp_lp_smoke/` |
| Yelp | `python run_yelp_lp_smoke.py`（调用 **`main_yelp.py`**） | `results/yelp_lp_smoke/` |

**`../README.md`** LP 表未给出 **`walk_num`**：**Link Prediction** 入口默认 **`--walk_num 100`**（见 `main.py` / `main_yelp.py`）。是否与论文 Table VIII 中叙述的默认 \(w\) 完全一致：**不确定**；敏感性脚本 **`run_lp_sensitivity_walk_num.py`** 通过 **`--walk_num`** 显式扫描。**Node Classification** 默认 **`walk_num=40`**，与 LP 不同。

**服务器正式 Link Prediction（汇总 `summary.csv` / `summary.txt`）**——推荐脚本（旧版 **`run_pubmed_lp_3seeds.py`** 仅历史兼容，正式文档以 **`*_lp_5seeds.py`** 为准）：

| 数据集 | 脚本 | 输出目录 |
|--------|------|----------|
| PubMed | `python run_pubmed_lp_5seeds.py` | `results/pubmed_lp_5seeds/` |
| DBLP | `python run_dblp_lp_5seeds.py` | `results/dblp_lp_5seeds/` |
| Yelp | `python run_yelp_lp_5seeds.py` | `results/yelp_lp_5seeds/` |

**Link Prediction 完整实验计划（服务器）**：PubMed LP **5 seeds** + DBLP LP **5 seeds** + Yelp LP **5 seeds**。

**Node Classification 完整实验计划（服务器）**：PubMed NC **`run_pubmed_5seeds.py`** + DBLP NC **`run_dblp_5seeds.py`** + Yelp NC **`run_yelp_5seeds.py`**（默认 5 seeds）。历史 **`run_yelp_3seeds.py`** / **`results/yelp_3seeds/`** 等为 **selected 3 seeds**，**不得**写成正式 5-seed 结论。

**MAG240M**：仍按 **`MAG240M运行说明.md`** 与根目录 **`scripts/`** 在 **Linux 服务器** 部署；本地不占位跑全量。

### 8.1 服务器批量命令摘录（与 `项目结构与使用说明.md` 一致）

**Node Classification**

```bash
cd ~/EHGNN/"Node Classification"
conda activate ehgnn

python run_pubmed_5seeds.py
python run_dblp_5seeds.py --seeds 42 3407 2026 6666 8888 --skip_existing
python run_yelp_5seeds.py --seeds 42 3407 2026 6666 8888 --skip_existing
```

**Link Prediction**

```bash
cd ~/EHGNN/"Link Prediction"
conda activate ehgnn

python run_pubmed_lp_5seeds.py --seeds 42 3407 2026 6666 8888 --skip_existing
python run_dblp_lp_5seeds.py --seeds 42 3407 2026 6666 8888 --skip_existing
python run_yelp_lp_5seeds.py --seeds 42 3407 2026 6666 8888 --skip_existing
```

**MAG240M**

```bash
cd ~/EHGNN
conda activate ehgnn

export MAG240_DATA_ROOT="$HOME/data"
python scripts/check_mag240m_data.py --root "$MAG240_DATA_ROOT"

chmod +x scripts/run_mag240m_server.sh
./scripts/run_mag240m_server.sh
```

---

## 9. 后续工作（建议）

| 方向 | 说明 |
|------|------|
| **DBLP LP** | **补跑** `run_dblp_lp_5seeds.py`（5 seeds）；排查 **`return_code=-15`**（SIGTERM/调度/资源等），生成 **`summary.txt` / `summary.csv`** |
| **Yelp LP** | 补跑 `run_yelp_lp_5seeds.py`；归档结果目录 |
| **MAG240M** | 按 **`MAG240M运行说明.md`** 单独执行；本批 core 归档无 MAG240M 完整结果 |
| 消融 / 敏感性（§0） | 服务器 **`server_run_extended_experiments.sh`**；**本批 core 未跑** |
| Fig.6 / IX / X | **`元路径实验待办.md`**、**`HPPR策略研究待办.md`** |
| 邻居策略研究 | 切换到 **`neighbor-strategy-dev`** 分支阅读脚本与历史 `results/` |
| 统计严谨性 | PubMed 测试集较小；归档 NC 汇总训练时长极短，写入论文前建议核对环境与数据路径 |

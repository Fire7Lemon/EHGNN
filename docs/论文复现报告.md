# 复现报告 — EHGNN

**Git 分支：`reproduce-baseline`。** 节点分类入口 **`Node Classification/main.py`** 仅保留论文口径：**RW 后为频次 Top-K**（`Counter.most_common(K)`）与 **`--r_neighbor` 随机邻居消融**。邻居策略扩展实验代码与脚本见 **`neighbor-strategy-dev`**。

---

## 1. 论文名称

**Efficient Learning for Billion-scale Heterogeneous Information Networks**（仓库 **`../README.md`** 所述实现）。

---

## 2. 复现目标

- 在 **PubMed / DBLP** 节点分类上按 **`../README.md`** 对齐命令运行 **`Node Classification/main.py`**；**Yelp** 走 **`Node Classification/main_yelp.py`**（多标签）。服务器正式 NC 批量默认 **5 seeds**。**服务器归档 `server_results/2026-05-16_core_reproduction/`** 已包含 PubMed / DBLP / Yelp NC 的 **5-seed** `summary`（见 **`实验记录.md`** §0.4）。**`results/yelp_3seeds/`** 等仍为历史 **selected 3-seed** 兼容目录，**不得**与上述正式 5-seed 归档混写为同一口径。  
- **Link Prediction**：本地仍可用 **`run_*_lp_smoke.py`** 做管线 smoke；**正式 Table VI 对齐**以服务器 **`run_*_lp_5seeds.py`** 产出为准。**Core reproduction（2026-05-16 服务器归档）**：**PubMed LP** 已有 **5-seed** `summary`（见 **`server_results/2026-05-16_core_reproduction/`** 与 **`实验记录.md`** §0.4）；**DBLP LP** 在该批run中 **未完成**（见 **`实验记录.md`** §0.1）；**Yelp LP** **缺失**。日志 **`precision`** / **`AP`** = **AP（Average Precision，平均精确率）**。旧版 **`run_pubmed_lp_3seeds.py`** 保留兼容。  
- PubMed NC：**多 seed**（`run_pubmed_5seeds.py`）、**消融**（`run_pubmed_ablation.py`）。DBLP NC：**`run_dblp_5seeds.py`**（默认或显式 5 seeds）。Yelp NC：**`run_yelp_5seeds.py`**；测试指标跨 seed 完全一致等现象见 **`实验记录.md`** §4，**不得**解读为强稳定性。  
- **MAG240M**：服务器侧部署说明（**`MAG240M运行说明.md`**、**`scripts/`**）；**完整训练计划在服务器执行**。一键主流程：`scripts/server_run_core_reproduction.sh`（日志 `logs/server_core_reproduction.log`）。**服务器归档 `server_results/2026-05-16_core_reproduction/` 未发现 MAG240M 完整结果**（见 **`实验记录.md`** §0.1）。
- **扩展实验（消融 / K / α / walk_num）**：脚本见 **`实验记录.md`** §0；服务器串联：`scripts/server_run_extended_experiments.sh`。**服务器归档 `server_results/2026-05-16_core_reproduction` 为 core reproduction，不包含该批消融与敏感性产物**。Fig.6 / Table IX / Table X 仍为 **TODO**（见 **`元路径实验待办.md`**、**`HPPR策略研究待办.md`**）。
- **不在本分支**：hybrid / temp / `neighbor_strategy` 扫描；相关内容仅在 **`neighbor-strategy-dev`** 与 **`实验记录.md`** 历史小节。

---

## 2.1 论文实验覆盖与执行状态（与代码同步）

状态含义：**Completed** = 服务器归档 **`server_results/2026-05-16_core_reproduction/`** 中已有可追溯 **summary**（综述 **`核心结果总结.md`**）；**Smoke-only** = 仅短跑验证管线；**Prepared** = 仓库已提供脚本与输出目录约定，**本批 core 未产出数值**；**Incomplete（运行中断）** = 有日志但无正式汇总或进程非正常结束（**不**自动归因于方法失败）；**Blocked-TODO** = **`元路径实验待办.md`** / **`HPPR策略研究待办.md`** 所列项。

| 论文目标 | 脚本 / 产物 | 状态 |
|----------|-------------|------|
| Table V PubMed NC | `run_pubmed_5seeds.py` → `pubmed_5seeds/` | **Completed**（归档 summary；见 **`实验记录.md`** §0.4、§2） |
| Table V DBLP NC（5 seeds） | `run_dblp_5seeds.py` → `dblp_5seeds/` | **Completed**（同上 §3） |
| Table V Yelp NC（5 seeds） | `run_yelp_5seeds.py` → `yelp_5seeds/` | **Completed**（同上 §4） |
| Table VI PubMed LP（5 seeds） | `run_pubmed_lp_5seeds.py` | **Completed**（归档 `pubmed_lp_5seeds/summary.txt`） |
| Table VI DBLP LP（5 seeds） | `run_dblp_lp_5seeds.py` | **Incomplete（运行中断，需补跑）** | 
| Table VI Yelp LP（5 seeds） | `run_yelp_lp_5seeds.py` | **Missing（本批未见 summary）** |
| Table VII NC/LP 消融 | `run_nc_ablation_all_datasets.py`、`run_lp_ablation_all_datasets.py` | **Prepared**；**本批 core 未运行** |
| Fig.2–3（K） | `run_nc_sensitivity_k.py`、`run_lp_sensitivity_k.py` | **Prepared**；**本批 core 未运行** |
| Fig.4–5（α） | `run_nc_sensitivity_alpha.py`、`run_lp_sensitivity_alpha.py` | **Prepared**；**本批 core 未运行** |
| Table VIII（walk_num） | `run_nc_sensitivity_walk_num.py`、`run_lp_sensitivity_walk_num.py` | **Prepared**；**本批 core 未运行** |
| Fig.6 / Table IX | — | **Blocked-TODO** |
| Table X（AvgSim / HeteSim） | — | **Blocked-TODO** |
| Runtime / Memory（Table V/VI 侧） | `Total training time` + `run_server_resource_monitor.sh` | **Partial**：时间可在日志/summary 中读取；**内存无代码内统一字段** |
| MAG240M | `scripts/check_mag240m_data.py`、`run_mag240m_server.sh` 等 | **Missing（本批归档）** |

**归档路径**：**`server_results/2026-05-16_core_reproduction/`**（core reproduction；**不含**消融与敏感性）。**服务器计划**：扩展实验 **`scripts/server_run_extended_experiments.sh`**（P1）；详见 **`项目结构与使用说明.md`** §10、`实验记录.md` §0.3。

---

## 3. 实验环境（记录模板）

以下为本仓库运行所需的典型依赖类别（**具体版本随机器 CUDA / PyTorch 轮子而变化**，需在目标环境中确认三角兼容）。**Git clone 后如何在服务器重建环境、`data/` 与 `results/` 为何不随仓库同步**：详见 **`环境配置说明.md`**。

| 组件 | 说明 |
|------|------|
| Python | 建议 3.10（参见 `MAG240M运行说明.md` 示例） |
| PyTorch | 与 CUDA 匹配的安装 |
| DGL | 与 PyTorch / CUDA 匹配的安装 |
| 其它 | `numpy`、`scipy`、`scikit-learn`、`ogb`；NC 模型依赖 **`torch_scatter`** |

**注意**：未在本报告中锁定某一固定 CUDA / torch / dgl 版本号；操作步骤与示例命令见 **`环境配置说明.md`** §3。提交给老师或协作者时，建议在 PR / issue 或附录中补充 **`pip freeze` 或 `conda export`** 片段。

---

## 4. 数据集来源与放置路径

| 数据集 | 来源 | 放置 |
|--------|------|------|
| PubMed / DBLP / Yelp | 仓库根目录 **`../README.md`** 中 Google Drive 链接 | 解压至 **`EHGNN/data/<DatasetName>/`**；运行时 **`Node Classification`** 下常用 **`--path ../data/`** |
| MAG240M | OGB-LSC（见 **`MAG240M运行说明.md`**） | 父目录由 **`MAG240MDataset(root=...)`** 指定；数据在 **`{root}/mag240m_kddcup2021/`** |

---

## 5. 复现流程（节点分类为主；§5.4 为 Link Prediction）

### 5.1 PubMed

1. 创建并激活 Conda / venv，安装 PyTorch、DGL、`torch_scatter`（版本匹配）、`ogb` 等。  
2. 将 PubMed 数据放入 **`data/PubMed/`**（含 `node.dat`、`link.dat`、`label.dat` 等，与 `utils.load_PubMed` 一致）。  
3. 执行：

```bash
cd Node Classification
python main.py --dataset PubMed --path ../data/ --seed 42
```

4. 批量复现：`run_pubmed_5seeds.py`、`run_pubmed_ablation.py`。服务器 NC 建议 **`run_pubmed_5seeds.py`** 默认跑满 **5 seeds**。

### 5.2 DBLP（README 超参；不写 `pubmed_nc_result.txt`）

1. 将 DBLP 数据放入 **`data/DBLP/`**（与 `utils.load_dblp` 一致）。  
2. README 表格对齐的单次示例（训练阶段计时见 `Total training time` 行；**数据加载与 RW 预计算另计且通常远长于 PubMed**）：

```bash
cd Node Classification
python main.py --dataset DBLP --path ../data/ --seed <seed> \
  --lr 5e-4 --dropout 0.5 --hidden 512 --n_layers 5 --batch_size 3000 --alpha 0.7 --K 20
```

3. 多 seed 汇总：`run_dblp_5seeds.py`（默认 **`[42, 3407, 2026, 6666, 8888]`** 或由 **`--seeds`**；**`--skip_existing`**），输出 **`results/dblp_5seeds/`**。**实验记录.md** §3 等处若仍为 **selected 3-seed** 数值，仅为历史切片；**正式服务器命令以完整 5 seeds 为准**。

### 5.3 Yelp（README 超参；`main_yelp.py`）

1. 将 Yelp 数据放入 **`data/Yelp/`**（与 `utils.load_Yelp` 一致）。  
2. README 对齐批量脚本：**服务器正式推荐 `run_yelp_5seeds.py`**（默认或 **`--seeds`**；可选 **`--skip_existing`**），输出 **`results/yelp_5seeds/`**。历史兼容：**`run_yelp_3seeds.py`** → **`results/yelp_3seeds/`**（**selected 3 seeds**，非正式 5-seed 口径）。单次示例：

```bash
cd Node Classification
python main_yelp.py --dataset Yelp --path ../data/ --seed <seed> \
  --alpha 0.7 --dropout 0.5 --K 20 --lr 0.0003 --hidden 256 --n_layers 4 --batch_size 3000
```

3. **测试 F1 跨 seed 近乎恒定等现象**（5-seed 服务器归档）见 **`实验记录.md`** §4。

### 5.4 Link Prediction（PubMed / DBLP / Yelp）

1. 数据放入 **`data/<Dataset>/`**，与 **`Link Prediction/utils`** 中对应加载函数一致。  
2. **本地 smoke**：在 **`Link Prediction/`** 使用 **`run_pubmed_lp_smoke.py`**、**`run_dblp_lp_smoke.py`**、**`run_yelp_lp_smoke.py`**（各 **`epochs=3`**、**`val_epochs=1`**）；**不作为论文完整复现指标**。  
3. **服务器 LP（各 5 seeds）**：**`run_pubmed_lp_5seeds.py`**、**`run_dblp_lp_5seeds.py`**、**`run_yelp_lp_5seeds.py`** → **`results/pubmed_lp_5seeds/`** 等；PubMed/DBLP 调 **`main.py`**，Yelp 调 **`Link Prediction/main_yelp.py`**。日志 **`precision`** / **`AP`** = Average Precision。旧 **`run_pubmed_lp_3seeds.py`** 保留兼容。  
4. **服务器命令清单**：**`实验记录.md`** §8.1、**`项目结构与使用说明.md`**。

---

## 6. 主要结果表格（服务器 core reproduction 归档）

本节 **Table V / 部分 Table VI** 数字取自 **`server_results/2026-05-16_core_reproduction/`** 下各 **`summary.txt`**（与 **`核心结果总结.md`**、`实验记录.md` §0.4 一致）。**Macro-F1**（Macro F1-score，宏平均 F1 值）、**Micro-F1**（Micro F1-score，微平均 F1 值）对应 NC 汇总的 best/final macro/micro；**AUC**（Area Under the Curve，曲线下面积）、**AP**（Average Precision，平均精确率）对应 LP 汇总。

| 实验 | 要点 |
|------|------|
| PubMed NC（5 seeds） | Best Macro-F1 **0.634410 ± 0.031795**；Best Micro-F1 **0.655814 ± 0.031417**；Final Macro-F1 **0.599828 ± 0.044462**；Final Micro-F1 **0.623256 ± 0.041601**；平均训练 **3.2780 s**。来源：`…/pubmed_5seeds/pubmed_5seeds_summary.txt`。 |
| DBLP NC（5 seeds） | Best Macro-F1 **0.155080 ± 0.011343**；Best Micro-F1 **0.391460 ± 0.014052**；Final Macro-F1 **0.156740 ± 0.022714**；Final Micro-F1 **0.386320 ± 0.022936**；avg train **5.5213 s**。来源：`…/dblp_5seeds/summary.txt`。 |
| Yelp NC（5 seeds） | Best/Final Macro-F1 **0.666300 ± 0.000000**；Best Micro-F1 **0.875820 ± 0.000045**；Final Micro-F1 **0.875800 ± 0.000000**；avg train **1512.8481 s**。来源：`…/yelp_5seeds/summary.txt`。跨 seed 测试 F1 近乎恒定等现象见 **`实验记录.md`** §4。 |
| PubMed LP（5 seeds） | Best AUC **0.554940 ± 0.005881**；Best AP **0.528140 ± 0.006090**；Final AUC **0.533960 ± 0.001596**；Final AP **0.516000 ± 0.000520**；avg time **1833.2327 s**。来源：`…/pubmed_lp_5seeds/summary.txt`。 |
| DBLP LP | **未完成**：仅有不完整 **`dblp_lp_seed_42.log`**；`logs/server_core_reproduction.log` 记载 **`return_code=-15`**；**无**正式 summary。**视为运行中断 / 需补跑**，**不**解释为方法失败。 |
| Yelp LP | **缺失**（本批归档未见 summary） |
| 消融（示例：本地历史 PubMed 3-seed） | 仍见 **`实验记录.md`** §5（**非**本批 core 归档）；**勿与 Table VII 全量自动化消融混为一谈** |
| 优化分支历史 sweep | **`实验记录.md`** §6（`neighbor-strategy-dev`） |

（上表 `…` = `server_results/2026-05-16_core_reproduction/Node Classification/results` 或 `…/Link Prediction/results`。）

---

## 7. 与论文结果对比

- 仓库根目录 **`../README.md`** **未嵌入** PubMed 等指标的具体数值表，仅给出超参建议。  
- **定量对比**需要对照论文 PDF / 官方补充材料中的表格，并注意：划分、预处理、随机种子、评测脚本是否与本文仓库 **完全一致**。  
- 消融中 **`--r_neighbor`** 相对默认频次 Top-K 的提升及优化分支下的 sweep 数值，见 **`实验记录.md`**；**不等于论文原文对默认方法的结论重述**。

---

## 8. 复现过程中已解决的问题（工程记录）

以下为项目推进中常见踩坑与对应处理方向（**不包含本轮对源码的修改记录**；具体修复以各次 commit / 本地日志为准）：

| 主题 | 说明 |
|------|------|
| DGL | 与 PyTorch / CUDA 版本对齐安装，避免 import / CUDA error |
| `torch_scatter` | NC 中 `models.py` 依赖 scatter；需安装与 torch 版本匹配的轮子 |
| NumPy ABI | 二进制扩展（如 scipy / dgl）与 NumPy 版本不匹配时的 **`numpy.dtype size changed`** 类错误；通过统一虚拟环境与版本约束缓解 |
| PubMed ID mapping | `load_PubMed` 中对全局 id 与类型块内紧凑 id 的映射（详见 **`Node Classification/utils.py`**）；缺失或错误会导致标签与特征错位 |
| SciPy sparse 索引 | 稀疏矩阵索引类型需与构建 CSR/CSC 时代码一致，避免隐式类型警告或索引错误 |
| DGL `random_walk` 与 seed | 节点 ID、`numpy`/`torch` 随机种子与 DGL 行为需在论文对齐实验中固定；注意种子参数类型与设备一致性 |

---

## 9. 当前局限

| 项目 | 说明 |
|------|------|
| MAG240M | **完整训练 / 全量数据**依赖大规模磁盘与内存；**服务器归档 `server_results/2026-05-16_core_reproduction` 未见完整 MAG240M 结果** |
| DBLP LP | **`server_core_reproduction.log`** 记载 **`return_code=-15`**；仅不完整 **`dblp_lp_seed_42.log`**；**无** LP summary — **需补跑**（视为运行中断） |
| Yelp LP | 本批归档 **Missing** |
| 扩展实验（Table VII / Fig.2–5 / VIII） | 脚本 **Prepared**；**本批 core 未跑**，产物依赖 **`server_run_extended_experiments.sh`** |
| 论文逐项对齐 | 若课程或审稿要求严格对齐，需逐项核对论文附录中的实现细节与评测协议 |

---

## 10. 文档与代码变更边界

- **`docs/`** 目录下的 **`论文复现报告.md`、`实验记录.md`、`项目结构与使用说明.md`、`GitHub提交检查清单.md`**（及 **`环境配置说明.md`**）：面向提交与老师阅读的说明材料。  
- **`reproduce-baseline`**：`Node Classification/main.py` 与 **`utils.py`** 以论文默认 RW–频次 Top-K 为准；模型 **`models.py`** 结构保持不变。邻居策略扩展仅在 **`neighbor-strategy-dev`**。

# 环境与数据准备说明（Git clone 之后）

**分支：`reproduce-baseline`。**  
本文说明：从 GitHub **clone 仓库后**，如何在**服务器或本机**重新创建可运行环境；以及 **`data/`、`results/` 等为何不随仓库同步**。仓库根目录的 **`environment.yml`**（conda 规格参考）与 **`requirements-freeze.txt`**（pip freeze 快照参考）若已提交，会随 clone 到达本地，但仍需手动创建环境并安装依赖（详见 **`environment.yml` 与 requirements-freeze.txt 的作用**）。

详细目录与命令参见 **`项目结构与使用说明.md`**；提交清单参见 **`GitHub提交检查清单.md`**。

---

## 1. Git clone 之后会拿到什么、仍然缺少什么

- **`git clone` 会拉取**：仓库内已提交的代码、脚本、Markdown 文档，以及**已跟踪的环境说明文件**（若已在仓库中），例如根目录的 **`environment.yml`**、**`requirements-freeze.txt`**。它们是**文字规格 / 快照**，不是已安装好的解释器与二进制包。
- **`git clone` 不会自动**：在本机或服务器上 **创建 conda 环境**，也**不会自动执行** `pip install` / `conda install` 安装 Python 依赖。
- **`git clone` 不会**：把你在其它机器上已经配置好的 **`ehgnn` conda 环境目录**（含 `site-packages`、CUDA 轮子缓存等）整体复制过来；服务器仍需自行 **`conda env create`**，或 **`conda create` + 按需 `pip install`**。
- **PyTorch**、**DGL**、**torch-scatter** 等与 CUDA 强相关的包：**不能**仅凭 clone 就就绪，必须在目标机器上按驱动与 PyTorch 官方通道选型安装；**也不宜**在未核对 CUDA 的情况下盲目完全照搬 **`requirements-freeze.txt`** 中的 GPU 相关版本（见下文 **`environment.yml` 与 `requirements-freeze.txt` 的作用**）。
- 文档或脚本里写的 **`conda activate ehgnn`**，含义是：**使用该名称的 conda 环境**；若目标机器上**尚未创建**名为 `ehgnn` 的环境，该命令会**失败**。

示例：

```bash
git clone <repo-url> EHGNN
cd EHGNN
conda activate ehgnn             # 若服务器还没有 ehgnn 环境，此处会失败
```

---

## environment.yml 与 requirements-freeze.txt 的作用

仓库根目录的 **`environment.yml`** 与 **`requirements-freeze.txt`** 是**环境重建参考**，用于记录规格与版本快照；它们**不是**“完整的 conda 环境本体”（不包含已编译安装后的环境目录），也**不能**替代你在服务器上对 CUDA / 驱动与轮子渠道的核对。

### 1. `environment.yml`

- **用途**：记录 conda 环境的**核心规格**（如 Python 版本、conda 通道等），便于在服务器上**从零创建**同名或等价环境。
- **示例**：

```bash
conda env create -f environment.yml
conda activate ehgnn
```

- 若该文件**未**列出全部 pip 依赖（或部分依赖需按 CUDA 单独选型），则还需参考 **`requirements-freeze.txt`** 做差异对照，或按 **`§3`**（PyTorch / DGL / torch-scatter）手动安装。
- **说明**：部分导出文件可能含本机路径字段（如 `prefix`）；在其它机器上使用 `conda env create -f` 时通常以新建环境为准，路径以目标机 conda 为准。

### 2. `requirements-freeze.txt`

- **用途**：记录本机 **`ehgnn`** 环境中 **`pip freeze`** 的**完整包版本快照**。
- **主要用于**：排查“本机能跑、服务器报错”时的版本差异，对照依赖是否一致。
- **不建议**：在服务器上**不加区分地**执行 `pip install -r requirements-freeze.txt`，尤其其中的 **PyTorch / DGL / torch-scatter / CUDA 相关**条目可能与服务器驱动或 wheel 源不匹配。
- **GPU 相关依赖**：应按服务器实际 CUDA / 驱动与 PyTorch 官方说明选择对应 **wheel**，再安装与之匹配的 DGL、torch-scatter 等。

### 3. 推荐服务器安装策略

1. **优先**用仓库规格创建 conda 环境：

```bash
conda env create -f environment.yml
conda activate ehgnn
```

2. 若创建失败，可改用手动创建（示例）：

```bash
conda create -n ehgnn python=3.10 -y
conda activate ehgnn
```

3. 然后按服务器 **CUDA** 安装 **PyTorch / DGL / torch-scatter**（版本与索引 URL 以官方文档为准，参见 **`§3`**）。

4. 最后安装通用依赖，例如：**numpy**、**scipy**、**scikit-learn**、**pandas**、**tqdm**、**ogb** 等（版本可与 freeze 对照，但以服务器兼容性为准）。

---

## 2. 服务器需要重新创建 ehgnn 环境

基础流程示例：

```bash
conda create -n ehgnn python=3.10 -y
conda activate ehgnn
python -m pip install --upgrade pip setuptools wheel
```

后续依赖**必须**根据服务器的 **CUDA 驱动版本**、是否安装 **GPU 版 PyTorch**、以及 **PyTorch 与 DGL / torch-scatter 的匹配关系**分别安装（见 **`§3`**）。**不要**假设与本机完全一致。

---

## 3. PyTorch / DGL / torch-scatter 需要按服务器 CUDA 版本安装

**要点：**

- **PyTorch、DGL、torch-scatter 不能盲目照搬本机 pip 列表**；需按服务器实际 **NVIDIA 驱动、CUDA 运行时、PyTorch 官方 wheel 通道**选择。
- 安装前建议在服务器上执行：

```bash
nvidia-smi
python --version
```

- **DGL** 与 **torch-scatter** 须与已安装的 **PyTorch 版本** 及 **CUDA 变体（cu118 / cu121 等）**一致。
- 若安装失败：**先保存完整报错与 `pip list`**，再查官方安装页；**不要随意混装**多个 CUDA 版本的 PyTorch/DGL。

以下命令仅为 **示例**（**CUDA 12.1 + PyTorch 2.3.1** 示意），**实际以服务器驱动与 PyTorch/DGL 官方文档为准**：

```bash
# 示例仅供 CUDA 12.1 / PyTorch 2.3.1 参考，实际以服务器为准
pip install torch==2.3.1 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
pip install dgl==2.2.1 -f https://data.dgl.ai/wheels/torch-2.3/cu121/repo.html
pip install torch-scatter -f https://data.pyg.org/whl/torch-2.3.1+cu121.html
pip install numpy==1.26.4 scipy scikit-learn pandas tqdm ogb
```

NC 任务还依赖 **`torch_scatter`**（与 PyTorch/CUDA 对齐）；若 wheel 源变更，请以 **[PyG wheels](https://data.pyg.org/whl/)** 或项目文档为准。

---

## 4. `data/` 和 `results/` 不随 GitHub 同步

- **`data/`**：通常由 **`.gitignore`** 忽略，**不会**出现在 clone 后的仓库里（除非刻意 `git add -f`，不推荐）。
- **`results/`**（含 **`Node Classification/results`**、**`Link Prediction/results`**，以及仓库根下可能出现的 **`results/`**）：同样通常被忽略，**不上传 GitHub**。
- **`logs/`**、模型 **checkpoint**、缓存文件：**不应**提交到 Git。

Clone 后需要**单独准备数据**（上传或下载）；实验产出写在本地 **`results/`**，由各脚本自动生成。

在服务器上检查目录为空是**正常现象**，不代表代码缺失：

```bash
ls data
ls "Node Classification/results"
ls "Link Prediction/results"
```

---

## 5. 数据准备方式

### 5.1 小型数据集（PubMed / DBLP / Yelp）

可从本机将已有 **`data/`** 目录上传到服务器，例如（路径按本机修改）：

```bash
scp -r "D:\procedure\SR\HGNN\EHGNN\data" <USER>@<SERVER_IP>:~/EHGNN/
```

服务器上检查：

```bash
cd ~/EHGNN
ls data/PubMed
ls data/DBLP
ls data/Yelp
```

数据来源见仓库根目录 **`README.md`**（Google Drive 链接）。

### 5.2 MAG240M

按 **`MAG240M运行说明.md`**，典型校验：

```bash
export MAG240_DATA_ROOT="$HOME/data"
python scripts/check_mag240m_data.py --root "$MAG240_DATA_ROOT"
```

**`MAG240MDataset(root=...)`** 会在首次使用时触发下载 / 预处理（耗时与磁盘占用大），需在文档所述路径下预留空间。

---

## 6. 本机 Windows：不 `conda activate` 也可用指定解释器

在 **PowerShell** 中可直接调用某环境下的 **`python.exe`**（等价于使用该环境的解释器与 `site-packages`），例如：

```powershell
& "$env:USERPROFILE\miniconda3\envs\ehgnn\python.exe" xxx.py
```

验证当前使用的解释器与环境前缀：

```powershell
& "$env:USERPROFILE\miniconda3\envs\ehgnn\python.exe" -c "import sys; print(sys.executable); print(sys.prefix)"
```

**Linux 服务器**上更推荐使用：

```bash
conda activate ehgnn
python xxx.py
```

或使用绝对路径（路径随 conda 安装位置变化）：

```bash
~/miniconda3/envs/ehgnn/bin/python xxx.py
```

---

## 7. 环境验证命令

在已激活环境或使用对应解释器后执行：

```bash
python - <<'PY'
import torch, dgl, numpy, scipy, sklearn
print("torch:", torch.__version__)
print("cuda available:", torch.cuda.is_available())
print("torch cuda:", torch.version.cuda)
print("gpu:", torch.cuda.get_device_name(0) if torch.cuda.is_available() else None)
print("dgl:", dgl.__version__)
print("numpy:", numpy.__version__)
print("scipy:", scipy.__version__)
PY
```

若某 import 失败，说明对应包未安装或版本与 CUDA 不匹配，需回到 §3 排查。

---

## 8. 推荐服务器运行前检查

```bash
nvidia-smi
df -h
free -h
git branch --show-current
git status
```

确认分支为预期（如 **`reproduce-baseline`**）、磁盘与内存充足，GPU 驱动可见。

---

## 9. 文档同步与交叉引用

| 文档 | 用途 |
|------|------|
| **`项目结构与使用说明.md`** | 目录结构、批量脚本与 **`results/`** 子目录约定 |
| **`README.md` / `MAG240M运行说明.md`** | 论文默认说明与 MAG240M 部署 |
| **`实验记录.md`** / **`论文复现报告.md`** | 实验记录与复现报告（**环境与本节对齐**） |
| **`GitHub提交检查清单.md`** | 提交前检查：`data/`、`results/`、`logs/` 不应提交；**`environment.yml` / `requirements-freeze.txt`** 的提交原则 |

---

## 10. 仍需用户手动确认的环境项

以下内容**无法**由仓库固定死，必须由你在目标机器上确认：

- **NVIDIA 驱动版本** 与 **`nvidia-smi`** 显示的 CUDA 兼容性。
- **PyTorch** 的 **CUDA 构建变体**（如 cu118 / cu121）是否与驱动匹配。
- **DGL**、**torch-scatter** 与 **PyTorch 次版本**、CUDA 变体的 **wheel 组合**。
- **Python 小版本**（如 3.10 vs 3.11）与上述二进制包的兼容性。
- **仅 CPU** 机器：需安装 CPU 版 PyTorch/DGL（与本示例中的 cu121 命令不同）。

如有新版本官方安装说明，**以 PyTorch / DGL / PyG 官网为准**，本文示例仅作流程演示。
